# @package _global_

# to execute this experiment run:
# uv run src/train.py experiment=data

defaults:
  - override /trainer: imagenet_vit_s_16
  - override /callbacks: default

seed: 42

# DATA TEAM EXPERIMENT (2 phase (low-res + high-res) training, no mixup/cutmix augmentation)
data:
  enable: false

experiment_name: "2_phase_training_no_mix"
tags: ["data", "train"]
